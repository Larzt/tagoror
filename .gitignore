# Compilación
build/
build-*/
cmake-build-*/
compile_commands.json
.cache/
*.o
*.so
*.a

# Artefactos de CMake fuera de build/
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
CTestTestfile.cmake
install_manifest.txt
Makefile
!makefile

# Ficheros generados por AUTOMOC
moc_*.cpp
*_autogen/
qrc_*.cpp
ui_*.h

# Editores e IDEs
.vscode/
.idea/
*.user
*.kdev4/
.qtc_clangd/
*.swp
*~
.DS_Store

# Paquetes generados
*.AppImage
*-installer.run
*.zip
dist/
dist-installer/
packaging/arch/src/
packaging/arch/pkg/
packaging/arch/*.pkg.tar.*
packaging/arch/*.tar.gz

# Datos de la app (notas, adjuntos de audio, tono de alarma)
notes.json
*.wav

# Cliente OAuth de Google (copia en Drive): nunca al repositorio público.
# Se pasa con google-oauth.mk (ver google-oauth.mk.example), -D o la CI.
google-oauth.mk
*.env
.env*
client_secret*.json
